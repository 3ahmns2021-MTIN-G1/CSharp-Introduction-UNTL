using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Deklaration einer Klasse

// public           -> access modifier (Zugriffsrechte) | keyword (vorreservierte Schlüsselwörter)
// class            -> keyword
// ExampleClass     -> Identifier (Name der Klasse)
// : MonoBehaviour  -> Erstellen von Unterklasse von MonoBehaviour
// {}               -> Scope/ Wirkungsbereich (was ist alles in einer klasse enthalten)





public class ExampleClass : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
